import styled from 'styled-components';

export const ScoreStyles = styled.div`

    .score {
        padding: 5px;
        width: 250px;
        border: 2px solid black;
        background: #007D00;
        color: #FFF;
    }
`
