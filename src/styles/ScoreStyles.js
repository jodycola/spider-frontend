import styled from 'styled-components';

export const ScoreStyles = styled.div`

.score {
        position: relative; 
        margin-left: auto;
        margin-right: auto;
        width: 250px;
        height: 90px;
        top: 10px;
        border: 2px solid black;
        background: #007D00;
        color: #FFF;
    }
`
